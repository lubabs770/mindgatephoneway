{
  "name": "mindgatephoneway",
  "version": "0.1.0",
  "private": true,
  "description": "Long-running Google Voice message capture via a persistent, stealth Chrome session. All config in config.js.",
  "main": "src/index.js",
  "scripts": {
    "bootstrap": "node src/bootstrap.js",
    "start": "node src/index.js",
    "dev": "MGP_HEADLESS=false node src/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^11.8.0",
    "dotenv": "^16.4.7",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2",
    "puppeteer": "^24.1.0"
  }
}
